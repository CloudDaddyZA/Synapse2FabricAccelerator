# Azure authentication (Service Principal). Leave empty to use Azure CLI / Managed Identity.
AZURE_TENANT_ID=
AZURE_CLIENT_ID=
AZURE_CLIENT_SECRET=

# Optional: default subscription scope (comma separated). CLI/config can override.
AZURE_SUBSCRIPTION_ID=
