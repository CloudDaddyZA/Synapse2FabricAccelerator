# Dashboard Agent
Static offline HTML dashboard (Chart.js) under `output/dashboard/{index.html,assets,data}` plus Power BI CSVs and `PowerBI_Model_Guide.md` under `powerbi/`. Sections: estate, regions, activity types, languages, Spark sizes, SQL SKUs, triggers, deps, security, complexity, readiness, waves, recommendations.
