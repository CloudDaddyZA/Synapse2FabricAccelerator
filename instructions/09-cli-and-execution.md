# CLI and Execution
Typer CLI: `python -m src.cli {discover|inventory|assess|migrate|report|dashboard|optimize|run-all}`. Config: `config/settings.example.yaml`, `config/settings.yaml`, `.env.example`. Auth: Azure CLI, Managed Identity, Service Principal via DefaultAzureCredential. Logs per agent under `output/logs/`.
