# Dashboard screenshots

Drop the dashboard / web-UI screenshots in this folder using the exact file names
below so the gallery in the top-level [`README.md`](../../README.md) renders.

| File | View |
|---|---|
| `overview.png` | Dashboard — Overview (KPI cards, resource summary, batch vs. real-time) |
| `fabric-readiness.png` | Dashboard — Fabric Readiness (delivery-team & timeline planner, complexity bands) |
| `pipeline-ops.png` | Dashboard — Pipeline Ops (run analytics, status breakdown, runs-by-day) |
| `data-integration.png` | Dashboard — Data Integration (pipelines, dataflows, triggers, datasets) |
| `data-engineering.png` | Dashboard — Data Engineering (Spark pools, notebooks) |
| `data-warehousing.png` | Dashboard — Data Warehousing (SQL pools) |
| `admin.png` | Dashboard — Admin (workspaces, integration runtimes, linked services) |
| `webapp-run.png` | Web UI — Run pipeline (stage progress, workspace selection, live logs) |
| `webapp-outputs.png` | Web UI — Generated outputs browser |
| `webapp-config.png` | Web UI — Azure sign-in & configuration (interactive login, settings saved to `config/settings.yaml`) |

PNG or JPG are both fine — keep the names but you may change the extension in the
README links if you use `.jpg`.
